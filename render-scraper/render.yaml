services:
  - type: web
    name: applix-render-scraper
    env: python
    plan: free
    rootDir: render-scraper
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /
